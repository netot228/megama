вся логика передачи информации между уровнями осуществляется через localStorage
все уровни вставляются в файл game через frame, в котором динамически меняется источник
связь между фреймом и родителем идет через PostMessage




инициализация пользователя проходит через форму, управление которой находится в
{addUserDataFromForm} from './staticpage_control.js';

там же подразумевается отправка данных пользователя на сервер при регистрации



showFinishWindow - после прохождения всех уровней в этой функции надо выполнить запрос на сервер,
чтобы получить данные по месту в рейтиге текущего пользователя
по коду раставлены комментарии
    в этом случае искать строку "// сделать запрос, получить место в рейтинге"



в модуле staticpage_control есть блок "// rating block"
там идет запрос внутрь себя, для теста к файлу ./js/ratingObject.json это как пример,
какой формат будет ожидаться после запроса